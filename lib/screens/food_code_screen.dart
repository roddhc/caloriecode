import 'package:flutter/material.dart';

class FoodCodeScreen extends StatelessWidget {
  const FoodCodeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Food Codes')),
      body: const Center(
        child: Text('Food Code Management (Placeholder)'),
      ),
    );
  }
}
